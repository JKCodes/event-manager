<% if object.errors.any? %>
  <div id="error_explanation">
    <h2>There are <%= pluralize(object.errors.count, "error") %> on this form: </h2>
    
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>